namespace TestPJS5;

public class TestProject
{
    /*
     * Robot robot = null;
     * Furnace furn = null;
     * Legs legs = null;
     * IWeapon leftWeap = null;
     * IWeapon rightWeap = null;
     * 
     * Pilot pPlayerPilot = null;
     * Pilot pBotPilot = null;
    */
    [Fact]
    public void TestBattle()
    {
        /*
         * legs = new Legs(1, "Basic Legs", 3, 2);
         * furn = new Furnace(1, "Normal Furnace", 3, 1, 50);
         * leftWeap = new MeleeWeapon(1, "Melee Weapon", 3, 1, 2, 15, 100, 0);
         * rightWeap = new NormalWeapon(1, "Basic Normal Weapon", 3, 1, 1, 15, 80, 40);
         * 
         * robotPlayer = new Robot(furn, legs, leftWeap, rightWeap);
         * robotBot = new Robot(furn, legs, leftWeap, rightWeap);
         * 
         * pPlayerPilot = new Player_Pilot(robotPlayer);
         * pBotPilot = new Bot_Pilot(robotBot);
         * 
         * int damage = robotPlayer.DealDamage(robotBot, 2, 2);
         * 
         * Assert.Equals(damage, 12);
         * Assert.True(robotBot.IsBroken());
         */
        
    }

    [Fact]
    public void TestWeapon()
    {
        /*
         *  leftWeap = new MeleeWeapon(1, "Melee Weapon", 3, 1, 2, 15, 100, 0);
         *  Assert.Equals(leftWeap.getType(), Weapon_Type.);
         *  
         *  rightWeap = new NormalWeapon(1, "Basic Normal Weapon", 3, 1, 1, 15, 80, 40);
         */
    }
}